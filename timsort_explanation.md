# TimSort 详解

## 1. 什么是 TimSort？

Timsort 是由 **Tim Peters** 在 2002 年为 Python 语言设计的排序算法，从 Python 2.3 开始成为 Python 的默认排序算法。后来被 **Java (JDK 1.7+)**、**Android SDK**、**Chrome V8 引擎 (JavaScript)**、**GNU Octave** 等广泛采用。

它是一种 **混合稳定排序算法**，融合了：
- **归并排序 (Merge Sort)** — 保证 O(n log n) 和稳定性
- **插入排序 (Insertion Sort)** — 在小数据量和部分有序数据上极快

## 2. 核心思想

### 2.1 Run（自然有序片段）
Timsort 扫描数组，识别出天然有序的子序列（称为 **run**）：
- **升序 run**: `[1, 3, 5, 7]`
- **降序 run**: `[9, 7, 5, 3]`（会被反转成升序）

### 2.2 最小 Run 长度
如果 run 的长度小于一个阈值（通常是 **32 或 64**），Timsort 会使用**插入排序**将该 run 扩展到最小长度，因为插入排序在小规模数据上非常高效。

### 2.3 合并策略（Galloping Mode）
Timsort 不是简单地两两合并，而是使用 **栈** 来跟踪 run，并维持以下两个不变量：
- `len(stack[-3]) > len(stack[-2]) + len(stack[-1])`
- `len(stack[-2]) > len(stack[-1])`

当违反上述条件时，会合并两个较短的 run。

合并时采用 **飞奔模式 (Galloping Mode)**：
- 当某个元素连续多次（默认 7 次）从同一侧被选中时，算法会切换到二分查找模式，一次性复制多个元素
- 这大大加速了**部分重叠数据**的合并过程

## 3. 时间复杂度

| 情况 | 时间复杂度 | 说明 |
|:---:|:---:|:---:|
| **最好** | **O(n)** | 数据已经有序 |
| **平均** | **O(n log n)** | 随机数据 |
| **最坏** | **O(n log n)** | 比快速排序更安全（快排最坏是 O(n²)） |

**空间复杂度**: O(n) — 需要额外数组用于合并

## 4. 为什么 TimSort 这么快？

| 特性 | 优势 |
|:---|:---|
| **识别自然有序片段** | 真实世界数据通常是部分有序的，Timsort 能充分利用这一点 |
| **小规模用插入排序** | 插入排序在小数组上比快速排序、归并排序都快 |
| **飞奔模式** | 当数据有规律时，可以一次复制多个元素，减少比较次数 |
| **稳定排序** | 保持相等元素的原始顺序，很多场景需要这个特性 |
| **自适应** | 自动适应数据的不同分布模式 |

## 5. 与 QuickSort 对比

| 对比项 | TimSort | QuickSort |
|:---:|::---:|:---:|
| 最坏情况 | **O(n log n)** | O(n²) |
| 最好情况 | **O(n)**（已有序） | O(n log n) |
| 稳定性 | ✅ 稳定 | ❌ 不稳定 |
| 空间 | O(n) | O(log n) |
| 部分有序数据 | **极快** | 一般 |
| 随机数据 | 稍慢于快排 | 最快 |

> 在随机数据上，Timsort 可能比 QuickSort 慢 10-20%，但在**部分有序数据**上可以快 **数倍**。
> 由于真实世界的数据往往是部分有序的，Timsort 的整体表现通常更好。
