# 会话备忘录 — 2026-05-10

## 用户背景

- 目前：测试工程师
- 技能：Linux、Python
- 目标：转 AI Agent 开发工程师
- 时间：有几个月可以系统学习

## 目标岗位核心要求

| 板块 | 要求 |
|---|---|
| 1. Agent 基础理论 | Plan、ReAct、Context、Memory |
| 2. Multi-Agent 编排 | DeepAgents、子智能体、循环逻辑、状态管理 |
| 3. 工具/插件系统 | MCP 协议、Tools/Skills 标准化 |
| 4. RAG & 知识库 | 多模态知识库、语义+关键词混合检索 |
| 5. 语音/长文本 | 语音链路、长文档 QA、深度研究 |
| 6. Vibe Coding | 用 AI 工具高效开发 |

## 学习计划（6 个阶段，总计 3-4 个月）

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1. Agent 理论基础 | LLM/Token/Prompt/Function Calling，手写 ReAct Agent | ✅ 完成 |
| 2. MCP + Tools | MCP 协议，用 Python 搭建 MCP Server，工具设计原则 | ✅ 完成 |
| 3. Multi-Agent 编排 | DeepAgents，子智能体，状态管理，循环逻辑 | ✅ 完成 |
| 4. RAG + 知识库 | 向量数据库，Embedding，混合检索，分块策略 | ✅ 完成 |
| 5. 长文本处理 | 长文档分块，多轮对话管理，Deep Research | 未开始 |
| 6. 综合项目 + Vibe Coding | 综合项目「AI 测试平台」，全程 AI 辅助开发 | 未开始 |

## 项目文件清单 (D:\agent_learning\)

```
hello_agent.py          # 阶段1：单 Agent（工具写死在代码里）
agent_with_mcp.py       # 阶段2：单 Agent + MCP（工具动态发现）
multi_agent.py          # 阶段3：Multi-Agent（Manager + 子Agent）
mcp_server.py           # MCP Server（list_dir, read_file, write_file）
rag_demo.py             # 阶段4：独立 RAG（建索引 + 查询）
rag_mcp_server.py       # 阶段4：RAG MCP Server（search_knowledge, reindex_knowledge）
agent_with_rag.py       # 阶段4：Agent + RAG（知识库检索 + LLM 回答）
session_2026-05-10.md   # 本备忘录
.chroma_db/             # ChromaDB 向量数据库（41 个文档块）
```

## 各阶段学到的核心概念

### 阶段1：Agent 基础
- **Agent = LLM + 工具 + 循环**
- **ReAct**：Reason(思考) → Act(行动) → Observe(观察) → 再思考
- **Context/History**：对话历史列表（messages），跨轮持久化就是 Memory
- **Tool**：Agent 的"手"，一个原子操作
- **Skill**：提前写好的流程模板（md），区别于 Tool 的可执行代码

### 阶段2：MCP + Tools
- **MCP**：Model Context Protocol，Agent 和工具之间解耦的标准协议
- **MCP Server**：独立进程，暴露工具（@mcp.tool()），通过 stdio/JSON-RPC 通信
- **核心价值**：加工具不改 Agent 代码，只需在 Server 端加函数 + 重启
- **ClientSession.list_tools()** → 动态发现工具 → 转成 OpenAI 工具格式
- **session.call_tool()** → 通过 MCP 协议调用工具执行

### 阶段3：Multi-Agent
- **Manager Agent**：自己不干活，只分配任务、汇总结果
- **Sub-Agent**：有独立的 ReAct 循环和上下文，只拿限定的工具
- **委派模式**：Manager 有一个 `delegate_sub_agent` 工具，用来派发子 Agent
- **汇总**：Manager 收集所有子 Agent 结果，合成最终回答

### 阶段4：RAG + 知识库
- **RAG**：Retrieve(检索) + Augmented(增强) + Generation(生成)
- **向量化**：sentence-transformers 本地模型（all-MiniLM-L6-v2），免费
- **ChromaDB**：本地持久化向量数据库
- **切块(Chunking)**：按行切（~30行/块，重叠5行），不乱切代码行
- **检索流程**：问题→向量化→ChromaDB找最相似3块→拼进Prompt→LLM回答
- **模型不接触数据库**：检索是 Python 代码做的，模型只看拼好的 Prompt

## 技术笔记

- **Windows Python 路径**：运行用 `D:\Python\python.exe`，不是 `python3`
- **CMD 编码问题**：`sys.stdout.reconfigure(encoding="utf-8", errors="replace")`
- **MCP 中文乱码**：MCP 管道通信会有编码问题，需用 `sanitize_string()` 清洗
- **DeepSeek API**：OpenAI 兼容格式，base_url="https://api.deepseek.com"，model="deepseek-chat"
- **API Key**：sk-248381b7b8a64de3879fccdfd2f0e213

## 用户要求

1. 学习中直接动手写代码，不要只讲概念
2. 偏好简洁直接的说明，不要过度解释
3. 进度存入此备忘录和 D:\agent_mem\

---

## 会话记录 — 2026-05-11（RAG 深度回顾 + 准备阶段5）

### 讨论内容

**RAG 三层架构回顾**：
- `rag_demo.py` — RAG 核心引擎（建索引 + 检索 + 生成），可独立命令行运行
- `rag_mcp_server.py` — 把 RAG 能力封装为 MCP 工具（search_knowledge, reindex_knowledge）
- `agent_with_rag.py` — Agent 通过 MCP stdio 连接 RAG Server，自主决策何时搜知识库

**设计思路深入**：
- 为什么按行切块（30行/5行重叠）——代码不适合按句号切，按行保护代码结构
- 为什么 top-3 而不是 1 或 10——信息量和噪声的平衡点
- Prompt 设计关键：告诉 LLM "找不到就说找不到"，防止编造
- MCP Server 用懒加载（模型只加载一次，长驻进程复用）
- 分层解耦：每层只管一件事，通过 MCP 协议连接

**检索链路详解（search 函数）**：
1. `model.encode([query])[0]` → 问题变成 384 维浮点向量
2. `collection.query(query_embeddings=[...], n_results=3)` → ChromaDB 逐块算余弦相似度，取 top-3
3. `.tolist()` 转换：numpy array → Python list（ChromaDB 不接受 numpy 类型）
4. `results["documents"][0]` → 双层列表，外层的 `[0]` 对应"第一个问题"

**什么是索引**：
- 索引 = 提前把 41 个代码块翻译成 41 组 384 维数字，存在 `.chroma_db/`
- 以前：关键词匹配，"怎么写" 对不上 `@mcp.tool()` → 找不到
- 现在：语义向量匹配，"怎么写" 的向量方向接近 `@mcp.tool()` 那段代码的向量方向 → 找到
- 核心：文本没法直接比"意思多接近"，数字可以比"方向多接近"

### 当前进度
- 阶段 1-4：✅ 完成
- 阶段 5（长文本处理）：下次开始
- 阶段 6（综合项目）：未开始

### 下次方向
阶段 5 三个子方向待选择：长文档分块 / 多轮对话管理 / Deep Research

---

## 会话记录 — 2026-05-12（阶段5-a：长文档分块）

### 核心成果

**通用切块模块 `chunkers.py`**：
- `CodeChunker`：按 `def/class/async def` 边界切代码，保护函数/类完整性
  - 大函数（>80行）二次切分，5行重叠保留上下文
  - 文件头部（imports、全局变量）自动识别为 `__header__` 块
- `DocumentChunker`：标题→段落两级切分
  - 先在 `#/##/###` 标题处切 section
  - 每个 section 内按空行切段落
  - 小段落合并到 ~500 字符目标大小
- 统一输出 `{id, content, doc_type, source, path}` 格式
- `chunk_directory()` 自动扫描目录，按扩展名分配切块器

**统一检索系统 `unified_rag.py`**：
- 代码和文档进同一个 ChromaDB collection（`unified_knowledge`）
- 每个 chunk 带 `doc_type` 元数据（"code" / "document"）
- 支持按类型过滤检索：`all` / `code` / `document`
- 交互式界面，可运行时切换检索范围

**测试素材**：
- `docs/mcp_protocol_guide.md` — MCP 协议完整指南（19 块）
- `docs/rag_systems_deep_dive.md` — RAG 系统深度解析（18 块）

### 关键技术点

**两种切块策略的本质区别**：
| | 代码 | 文档 |
|---|---|---|
| 边界依据 | AST/正则匹配函数定义行 | 标题层级 + 段落空行 |
| 最小单元 | 一个函数/类 | 一个段落（合并后~500字） |
| 重叠策略 | 大函数内部重叠 | 段落间不重叠（标题已保证上下文） |
| 元数据 | element（函数名）、行号 | section（章节标题）、chunk_index |

**类型过滤的价值**：
- `search_knowledge 函数怎么实现的` → all 模式下偏向文档（文档里也讨论了 search）
- 切换到 `code` 过滤 → 直接定位到 `rag_mcp_server.py` 的函数实现
- **混合检索（向量+BM25）** 能进一步解决函数名精确匹配问题

**架构分层**：
```
chunkers.py          — 切块策略层（纯数据转换）
  ↓
unified_rag.py       — 索引+检索层（向量化、ChromaDB、问答）
  ↓
（后续）MCP Server   — 服务化层
```

### 当前进度
- 阶段 1-4：✅
- 阶段 5-a（长文档分块）：✅ 完成
- 阶段 5-b（多轮对话管理）：进行中（memory_manager 已写，待实测）
- 阶段 5-c（Deep Research）：未开始
- 阶段 6（综合项目）：未开始

### 新增文件
```
chunkers.py              # 通用切块器（CodeChunker + DocumentChunker）
unified_rag.py           # 统一 RAG 检索系统
test_unified_rag.py      # 检索测试脚本
docs/mcp_protocol_guide.md      # 测试文档1
docs/rag_systems_deep_dive.md   # 测试文档2
```

---

## 会话记录 — 2026-05-12 续（阶段5-b：多轮对话管理）

### 核心概念

**问题**：Agent 每轮对话都把完整历史发给 LLM，消息量线性增长。
- 第 50 轮时 90% 输入 token 是历史，浪费钱
- 极端情况下超出模型上下文窗口，直接报错

**两根支柱**：
| 策略 | 做法 | 缺点 |
|------|------|------|
| 滑动窗口 | 只保留最近 N 条原文 | 丢历史，用户引旧内容时失忆 |
| 摘要压缩 | LLM 提取前文大意 | 丢细节，代码/数据可能被简化 |

**实际方案**：两者结合——旧消息压成摘要，新消息保留原文。

### 设计决策

- **阈值**：消息条数 > 10 触发压缩
- **摘要位置**：`system` 角色，插在列表最前面
- **摘要内容**：核心需求、决定、代码改动、待解决问题
- **压缩方式**：LLM（deepseek-chat，temperature=0.3 确保稳定）
- **分层摘要**：再次溢出时，旧摘要和新旧消息一起再压（摘要的摘要）

### 实现文件

- `memory_manager.py` — MemoryManager 类
  - `compress(old_messages)` → LLM 压缩成 ≤300 字摘要
  - `manage(messages)` → 返回 `[system摘要, ...最近N条]`
  - `should_compress(messages)` → 判断是否超阈值
- 已接入 `agent_with_mcp.py`（3 处改动，共 4 行）
- 待实测：连续对话看压缩效果

### 已知待改进

1. 缺少原始系统指令的保护（现有 Agent 没加系统指令，暂时不影响）
2. 阈值按消息条数而非 token 数（token 更精确）
3. 摘要中可能丢失代码细节（后续可单独保留代码片段）

---

## 会话记录 — 2026-05-13（阶段5-c：Deep Research）

### 核心成果

**Deep Research Agent `deep_research.py`**：
- 完整实现多步自主调研：拆解 → 双源搜索 → 追问深挖 → 综合报告
- 搜索源：Bing（HTML 抓取，免费）+ 本地 ChromaDB 知识库
- 5 步闭环：decompose → search_all → assess_and_follow_up → answer_sub_question → synthesize

### 架构

```
用户问题
  → [1. 拆解] LLM 拆成 3-5 个子问题
  → [2. 逐个调研] 对每个子问题：
       → search_all() → Bing + ChromaDB 双源搜索
       → assess_and_follow_up() → LLM 判断信息是否充分
       → 不充分 → 生成追问 → 再搜索（最多 2 轮）
       → answer_sub_question() → 综合材料给阶段性回答
  → [3. 综合报告] synthesize() → 结构化最终报告
```

### 关键技术点

**Bing 搜索解析**：
- 免费，无需 API Key，httpx 发 GET 请求
- HTML 正则提取：`<li class="b_algo">` 块 → title + cite + b_caption
- 国外搜索引擎（DuckDuckGo/Wikipedia）在国内均被墙，Bing 可用

**追问循环的价值**：
- 子问题 4（通信方式差异）触发了 3 个追问，材料从 6 份扩展到 18 份
- LLM 自动生成了关于 JSON-RPC、数据格式、协议标准的具体追问
- 这就是 Deep Research 区别于单次 RAG 的核心

**搜索源可插拔**：
- 每个搜索源实现 `search(query) → list[dict]` 接口
- 新增搜索源只需加函数，不改主流程

### 当前进度
- 阶段 1-4：✅
- 阶段 5-a（长文档分块）：✅
- 阶段 5-b（多轮对话管理）：✅ 实测完成
- 阶段 5-c（Deep Research）：✅ 完成
- 阶段 6（综合项目）：进行中

### 新增文件
```
deep_research.py         # Deep Research Agent（Bing + ChromaDB）
test_memory_integration.py  # 多轮对话集成测试
```

### 设计改进（2026-05-13）
- 原子问题兜底：拆解失败时直接用原问题作为唯一子问题，不再让用户重新输入
- 搜索源：Bing（HTML 抓取，免费）替代 DuckDuckGo（被墙）
- 来源链接：提取 Bing 显示 URL（cite 标签），放弃追踪链接解码

---

## 会话记录 — 2026-05-14（阶段5-b 实测完成）

### 实测结果
- `test_memory.py`：13 条消息 → 1 摘要 + 5 条原文，基础压缩 ✅
- `test_memory_integration.py`：基础压缩 + 二次压缩（分层摘要）+ 边界情况，全部 ✅

### Bug 修复
- **问题**：`memory_manager.compress()` 只读 `content` 字段，assistant 消息调工具时 `content=None`，工具调用声明丢失
- **根因**：OpenAI API 的 `content` 和 `tool_calls` 是互斥的两个输出通道
- **修复**：`content` 为空时检查 `tool_calls`，兼容 OpenAI 标准格式 `[{function: {name, arguments}}]` 和简化字符串格式

### 当前进度
- 阶段 1-4：✅
- 阶段 5-a：✅
- 阶段 5-b：✅ 实测完成
- 阶段 5-c：✅
- 阶段 6：待明确需求

---

## 阶段 6 预备 — 2026-05-13

综合项目「AI 测试平台」，待下次明确需求后开始。
核心待澄清：给谁用、做什么、解决什么痛点。
